<?php

// Code generated by @ui8kit/codegen. DO NOT EDIT.

declare(strict_types=1);

namespace UI8Kit;

final class Classes
{
    private const ALERT_ALERT = ['base' => 'rounded-lg border px-4 py-3 shadow-sm outline-none ring-offset-background gap-6', 'keys' => ['variant'], 'defaults' => ['variant' => 'default'], 'byKey' => ['variant' => ['default' => 'bg-card text-card-foreground', 'destructive' => 'border-destructive bg-destructive/10 text-destructive', 'success' => 'border-primary bg-primary/10 text-primary', 'warning' => 'border-border bg-accent text-accent-foreground']]];
    private const BADGE_BADGE = ['base' => 'inline-flex items-center rounded-full border px-2 py-1 text-xs font-semibold transition-colors focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2', 'keys' => ['variant', 'size'], 'defaults' => ['size' => 'default', 'variant' => 'default'], 'byKey' => ['size' => ['default' => 'px-2 py-1 text-xs', 'lg' => 'px-3 py-1 text-sm', 'sm' => 'px-2 py-1 text-xs'], 'variant' => ['default' => 'border-transparent bg-primary text-primary-foreground hover:bg-primary/80', 'destructive' => 'border-transparent bg-destructive text-destructive-foreground hover:bg-destructive/80', 'outline' => 'text-foreground', 'secondary' => 'border-transparent bg-secondary text-secondary-foreground hover:bg-secondary/80']]];
    private const BLOCK_BLOCK = ['base' => '', 'keys' => [], 'defaults' => [], 'byKey' => []];
    private const BOX_BOX = ['base' => '', 'keys' => [], 'defaults' => [], 'byKey' => []];
    private const BUTTON_BUTTON = ['base' => 'inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50', 'keys' => ['variant', 'size'], 'defaults' => ['size' => 'default', 'variant' => 'default'], 'byKey' => ['size' => ['default' => 'h-10 px-4 py-2', 'icon' => 'h-10 w-10', 'lg' => 'h-12 rounded-md px-8', 'sm' => 'h-8 rounded-md px-3'], 'variant' => ['default' => 'bg-primary text-primary-foreground hover:bg-primary/90', 'destructive' => 'bg-destructive text-destructive-foreground hover:bg-destructive/90', 'ghost' => 'hover:bg-accent hover:text-accent-foreground', 'link' => 'text-primary underline-offset-4 hover:underline', 'outline' => 'border border-input bg-background hover:bg-accent hover:text-accent-foreground', 'secondary' => 'bg-secondary text-secondary-foreground hover:bg-secondary/80']]];
    private const CARD_CARD = ['base' => 'rounded-md border border-border bg-card text-card-foreground shadow-sm', 'keys' => ['variant'], 'defaults' => ['variant' => 'default'], 'byKey' => ['variant' => ['default' => '', 'raised' => '', 'kpi' => 'bg-card/50', 'muted' => 'bg-muted/40', 'ghost' => 'border-dashed bg-background shadow-none', 'compact' => 'p-4', 'flat' => 'shadow-none', 'accent' => 'border-transparent bg-accent/30']]];
    private const CHECKBOX_CHECKBOX = ['base' => 'rounded border border-primary text-primary focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50', 'keys' => ['variant', 'size'], 'defaults' => ['size' => 'default', 'variant' => 'default'], 'byKey' => ['size' => ['default' => 'h-4 w-4', 'lg' => 'h-5 w-5', 'sm' => 'h-4 w-4', 'xs' => 'h-3 w-3'], 'variant' => ['default' => 'border-primary', 'ghost' => 'border-primary bg-muted/30', 'outline' => 'border-primary']]];
    private const CONTAINER_CONTAINER = ['base' => '', 'keys' => [], 'defaults' => [], 'byKey' => []];
    private const DIALOG_DIALOG = ['base' => 'm-auto rounded-lg border border-border bg-background text-foreground shadow-lg backdrop:bg-background/80', 'keys' => ['variant', 'size'], 'defaults' => ['size' => 'default', 'variant' => 'default'], 'byKey' => ['size' => ['default' => 'w-full max-w-lg', 'full' => 'h-full w-full max-w-none', 'lg' => 'w-full max-w-2xl', 'sm' => 'w-full max-w-sm', 'xl' => 'w-full max-w-4xl'], 'variant' => ['card' => 'bg-card text-card-foreground', 'default' => '', 'sheet' => 'min-h-screen rounded-none']]];
    private const DISCLOSURE_DISCLOSURE = ['base' => 'group', 'keys' => ['variant', 'size'], 'defaults' => ['size' => 'default', 'variant' => 'default'], 'byKey' => ['size' => ['default' => 'text-sm', 'lg' => 'text-base', 'sm' => 'text-sm'], 'variant' => ['card' => 'rounded-md border border-border bg-card text-card-foreground', 'default' => 'rounded-md border border-border', 'ghost' => '']]];
    private const DISCLOSURE_SUMMARY = ['base' => 'flex cursor-pointer list-none items-center justify-between gap-2 [&::-webkit-details-marker]:hidden', 'keys' => ['variant', 'size'], 'defaults' => ['size' => 'default', 'variant' => 'default'], 'byKey' => ['size' => ['default' => 'text-sm', 'lg' => 'text-base', 'sm' => 'text-sm'], 'variant' => ['card' => 'px-4 py-3 font-medium', 'default' => 'px-4 py-3 font-medium', 'ghost' => 'py-2 font-medium']]];
    private const FORM_FORM = ['base' => 'grid gap-4', 'keys' => [], 'defaults' => [], 'byKey' => []];
    private const FORM_FORM_ITEM = ['base' => 'flex flex-col gap-2', 'keys' => [], 'defaults' => [], 'byKey' => []];
    private const FORM_FORM_DESCRIPTION = ['base' => 'text-xs text-muted-foreground', 'keys' => [], 'defaults' => [], 'byKey' => []];
    private const FORM_FORM_MESSAGE = ['base' => 'text-sm font-medium text-destructive', 'keys' => [], 'defaults' => [], 'byKey' => []];
    private const CONTROLS_FIELDSET = ['base' => 'grid gap-4', 'keys' => ['variant', 'size'], 'defaults' => ['size' => 'default', 'variant' => 'default'], 'byKey' => ['size' => ['default' => 'gap-4', 'lg' => 'gap-6', 'sm' => 'gap-2'], 'variant' => ['card' => 'rounded-md border border-border p-4', 'default' => '', 'inline' => 'flex flex-wrap items-start gap-4']]];
    private const CONTROLS_LEGEND = ['base' => 'text-sm font-medium', 'keys' => ['variant', 'size'], 'defaults' => ['size' => 'default', 'variant' => 'default'], 'byKey' => ['size' => ['default' => 'text-sm', 'lg' => 'text-base', 'sm' => 'text-xs'], 'variant' => ['default' => '', 'muted' => 'text-muted-foreground']]];
    private const CONTROLS_METER = ['base' => 'h-2 w-full', 'keys' => ['variant', 'size'], 'defaults' => ['size' => 'default', 'variant' => 'default'], 'byKey' => ['size' => ['default' => 'h-2', 'lg' => 'h-3', 'sm' => 'h-1'], 'variant' => ['default' => 'accent-primary', 'muted' => 'accent-muted-foreground']]];
    private const CONTROLS_PROGRESS = ['base' => 'h-2 w-full', 'keys' => ['variant', 'size'], 'defaults' => ['size' => 'default', 'variant' => 'default'], 'byKey' => ['size' => ['default' => 'h-2', 'lg' => 'h-3', 'sm' => 'h-1'], 'variant' => ['default' => 'accent-primary', 'muted' => 'accent-muted-foreground']]];
    private const GRID_GRID = ['base' => 'grid', 'keys' => [], 'defaults' => [], 'byKey' => []];
    private const GROUP_GROUP = ['base' => 'flex min-w-0', 'keys' => [], 'defaults' => [], 'byKey' => []];
    private const ICON_ICON = ['base' => 'inline-block shrink-0', 'keys' => ['type', 'size'], 'defaults' => ['size' => 'default', 'type' => 'class'], 'byKey' => ['size' => ['default' => 'h-4 w-4', 'lg' => 'h-6 w-6', 'md' => 'h-5 w-5', 'sm' => 'h-4 w-4', 'xl' => 'h-8 w-8', 'xs' => 'h-3 w-3'], 'type' => ['class' => '', 'svg' => '', 'text' => '']]];
    private const ICONBADGE_ICONBADGE = ['base' => 'inline-flex shrink-0 items-center justify-center rounded-md font-semibold', 'keys' => ['variant', 'size'], 'defaults' => ['variant' => 'default', 'size' => 'default'], 'byKey' => ['variant' => ['default' => 'bg-muted text-muted-foreground', 'accent' => 'bg-accent text-accent-foreground', 'secondary' => 'bg-secondary text-secondary-foreground', 'outline' => 'border border-border text-foreground', 'destructive' => 'bg-destructive text-destructive-foreground'], 'size' => ['xs' => 'h-6 w-6 text-xs', 'sm' => 'h-8 w-8 text-xs', 'default' => 'h-10 w-10 text-sm', 'lg' => 'h-12 w-12 text-base']]];
    private const IMAGE_IMAGE = ['base' => 'block max-w-full', 'keys' => ['variant', 'size', 'fit', 'position', 'aspect'], 'defaults' => ['aspect' => 'auto', 'fit' => 'cover', 'position' => 'center', 'size' => 'auto', 'variant' => 'default'], 'byKey' => ['aspect' => ['auto' => 'aspect-auto', 'square' => 'aspect-square', 'video' => 'aspect-video'], 'fit' => ['contain' => 'object-contain', 'cover' => 'object-cover', 'fill' => 'object-fill', 'none' => 'object-none', 'scale-down' => 'object-scale-down'], 'position' => ['bottom' => 'object-bottom', 'center' => 'object-center', 'left' => 'object-left', 'left-bottom' => 'object-left-bottom', 'left-top' => 'object-left-top', 'right' => 'object-right', 'right-bottom' => 'object-right-bottom', 'right-top' => 'object-right-top', 'top' => 'object-top'], 'size' => ['auto' => '', 'default' => '', 'full' => 'h-full w-full', 'lg' => 'h-16 w-16', 'md' => 'h-12 w-12', 'sm' => 'h-8 w-8', 'xl' => 'h-24 w-24', 'xs' => 'h-6 w-6'], 'variant' => ['avatar' => 'rounded-full', 'default' => '', 'logo' => 'object-contain', 'rounded' => 'rounded-md', 'thumbnail' => 'rounded-md border border-border']]];
    private const INLINE_INLINE = ['base' => '', 'keys' => [], 'defaults' => [], 'byKey' => []];
    private const INPUT_INPUT = ['base' => 'w-full rounded-md border px-3 py-2 text-sm outline-none transition-colors focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50', 'keys' => ['variant', 'size'], 'defaults' => ['size' => 'default', 'variant' => 'default'], 'byKey' => ['size' => ['default' => 'h-10 px-3 py-2 text-sm', 'lg' => 'h-12 px-4 text-base', 'sm' => 'h-8 px-3 text-sm', 'xs' => 'h-8 px-2 text-xs'], 'variant' => ['default' => 'border-border bg-background', 'ghost' => 'border-transparent bg-muted', 'outline' => 'border-border bg-background']]];
    private const LABEL_LABEL = ['base' => 'text-sm font-medium leading-none', 'keys' => [], 'defaults' => [], 'byKey' => []];
    private const LINEBREAK_LINEBREAK = ['base' => '', 'keys' => [], 'defaults' => [], 'byKey' => []];
    private const LINK_LINK = ['base' => 'inline-flex items-center gap-2 underline-offset-4 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2', 'keys' => ['variant', 'size'], 'defaults' => ['size' => 'default', 'variant' => 'default'], 'byKey' => ['size' => ['default' => 'text-sm', 'lg' => 'text-lg', 'md' => 'text-base', 'sm' => 'text-sm'], 'variant' => ['block' => 'flex w-full text-foreground hover:text-primary', 'default' => 'text-primary hover:underline', 'external' => 'text-primary hover:underline', 'muted' => 'text-muted-foreground hover:text-foreground hover:underline', 'nav' => 'text-foreground hover:text-primary']]];
    private const LIST_LIST = ['base' => '', 'keys' => [], 'defaults' => [], 'byKey' => []];
    private const RADIO_RADIO = ['base' => 'rounded border border-primary text-primary focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50', 'keys' => ['variant', 'size'], 'defaults' => ['size' => 'default', 'variant' => 'default'], 'byKey' => ['size' => ['default' => 'h-4 w-4', 'lg' => 'h-5 w-5', 'sm' => 'h-4 w-4', 'xs' => 'h-3 w-3'], 'variant' => ['default' => 'border-primary', 'ghost' => 'border-primary bg-muted/30', 'outline' => 'border-primary']]];
    private const SELECT_SELECT = ['base' => 'w-full rounded-md border px-3 py-2 text-sm outline-none transition-colors focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50', 'keys' => ['variant', 'size'], 'defaults' => ['size' => 'default', 'variant' => 'default'], 'byKey' => ['size' => ['default' => 'h-10 px-3 py-2 text-sm', 'lg' => 'h-12 px-4 text-base', 'sm' => 'h-8 px-3 text-sm', 'xs' => 'h-8 px-2 text-xs'], 'variant' => ['default' => 'border-border bg-background', 'ghost' => 'border-transparent bg-muted', 'outline' => 'border-border bg-background']]];
    private const SEPARATOR_SEPARATOR = ['base' => 'shrink-0 border-0 bg-border', 'keys' => ['orientation', 'variant'], 'defaults' => ['orientation' => 'horizontal', 'variant' => 'default'], 'byKey' => ['orientation' => ['horizontal' => 'h-px w-full', 'vertical' => 'h-full w-px'], 'variant' => ['default' => '', 'muted' => 'bg-muted', 'spaced' => 'my-4', 'strong' => 'bg-foreground']]];
    private const STACK_STACK = ['base' => 'flex flex-col items-start justify-start', 'keys' => [], 'defaults' => [], 'byKey' => []];
    private const SWITCH_SWITCH = ['base' => 'rounded border border-primary text-primary focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50', 'keys' => ['variant', 'size'], 'defaults' => ['size' => 'default', 'variant' => 'default'], 'byKey' => ['size' => ['default' => 'h-4 w-4', 'lg' => 'h-5 w-5', 'sm' => 'h-4 w-4', 'xs' => 'h-3 w-3'], 'variant' => ['default' => 'border-primary', 'ghost' => 'border-primary bg-muted/30', 'outline' => 'border-primary']]];
    private const TABLE_TABLE = ['base' => '', 'keys' => [], 'defaults' => [], 'byKey' => []];
    private const TEXT_TEXT = ['base' => '', 'keys' => [], 'defaults' => [], 'byKey' => []];
    private const TEXTAREA_TEXTAREA = ['base' => 'w-full rounded-md border px-3 py-2 text-sm outline-none transition-colors focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50', 'keys' => ['variant', 'size'], 'defaults' => ['size' => 'default', 'variant' => 'default'], 'byKey' => ['size' => ['default' => 'h-10 px-3 py-2 text-sm', 'lg' => 'h-12 px-4 text-base', 'sm' => 'h-8 px-3 text-sm', 'xs' => 'h-8 px-2 text-xs'], 'variant' => ['default' => 'border-border bg-background', 'ghost' => 'border-transparent bg-muted', 'outline' => 'border-border bg-background']]];
    private const TITLE_TITLE = ['base' => '', 'keys' => [], 'defaults' => [], 'byKey' => []];

    /** Alert class composition — the server-side asChild substitute. */
    public static function alertClasses(array $in = []): string
    {
        return Rt::compose(self::ALERT_ALERT, ['variant' => (string) ($in['variant'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** Badge class composition — the server-side asChild substitute. */
    public static function badgeClasses(array $in = []): string
    {
        return Rt::compose(self::BADGE_BADGE, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** Block class composition — the server-side asChild substitute. */
    public static function blockClasses(array $in = []): string
    {
        return Rt::compose(self::BLOCK_BLOCK, [], (string) ($in['class'] ?? ''));
    }

    /** Box class composition — the server-side asChild substitute. */
    public static function boxClasses(array $in = []): string
    {
        return Rt::compose(self::BOX_BOX, [], (string) ($in['class'] ?? ''));
    }

    /** Breadcrumb class composition — the server-side asChild substitute. */
    public static function breadcrumbClasses(array $in = []): string
    {
        return Rt::cn('text-sm', (string) ($in['class'] ?? ''));
    }

    /** Button class composition — the server-side asChild substitute. */
    public static function buttonClasses(array $in = []): string
    {
        return Rt::compose(self::BUTTON_BUTTON, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (((bool) ($in['disabled'] ?? false)) ? 'pointer-events-none opacity-50' : ''), (string) ($in['class'] ?? ''));
    }

    /** Card class composition — the server-side asChild substitute. */
    public static function cardClasses(array $in = []): string
    {
        return Rt::compose(self::CARD_CARD, ['variant' => (string) ($in['variant'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** CardHeader class composition — the server-side asChild substitute. */
    public static function cardHeaderClasses(array $in = []): string
    {
        return Rt::cn('border-b border-border px-4 py-2', (string) ($in['class'] ?? ''));
    }

    /** CardTitle class composition — the server-side asChild substitute. */
    public static function cardTitleClasses(array $in = []): string
    {
        return Rt::cn('text-xl font-semibold tracking-tight', (string) ($in['class'] ?? ''));
    }

    /** CardDescription class composition — the server-side asChild substitute. */
    public static function cardDescriptionClasses(array $in = []): string
    {
        return Rt::cn('text-sm text-muted-foreground', (string) ($in['class'] ?? ''));
    }

    /** CardContent class composition — the server-side asChild substitute. */
    public static function cardContentClasses(array $in = []): string
    {
        return Rt::cn('p-4', (string) ($in['class'] ?? ''));
    }

    /** CardFooter class composition — the server-side asChild substitute. */
    public static function cardFooterClasses(array $in = []): string
    {
        return Rt::cn('border-t border-border px-4 py-3', (string) ($in['class'] ?? ''));
    }

    /** Checkbox class composition — the server-side asChild substitute. */
    public static function checkboxClasses(array $in = []): string
    {
        return Rt::compose(self::CHECKBOX_CHECKBOX, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** Container class composition — the server-side asChild substitute. */
    public static function containerClasses(array $in = []): string
    {
        return Rt::compose(self::CONTAINER_CONTAINER, [], (string) ($in['class'] ?? ''));
    }

    /** Dialog class composition — the server-side asChild substitute. */
    public static function dialogClasses(array $in = []): string
    {
        return Rt::compose(self::DIALOG_DIALOG, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** Disclosure class composition — the server-side asChild substitute. */
    public static function disclosureClasses(array $in = []): string
    {
        return Rt::compose(self::DISCLOSURE_DISCLOSURE, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** Summary class composition — the server-side asChild substitute. */
    public static function summaryClasses(array $in = []): string
    {
        return Rt::compose(self::DISCLOSURE_SUMMARY, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** Form class composition — the server-side asChild substitute. */
    public static function formClasses(array $in = []): string
    {
        return Rt::compose(self::FORM_FORM, [], (string) ($in['class'] ?? ''));
    }

    /** FormItem class composition — the server-side asChild substitute. */
    public static function formItemClasses(array $in = []): string
    {
        return Rt::compose(self::FORM_FORM_ITEM, [], (string) ($in['class'] ?? ''));
    }

    /** FormDescription class composition — the server-side asChild substitute. */
    public static function formDescriptionClasses(array $in = []): string
    {
        return Rt::compose(self::FORM_FORM_DESCRIPTION, [], (string) ($in['class'] ?? ''));
    }

    /** FormMessage class composition — the server-side asChild substitute. */
    public static function formMessageClasses(array $in = []): string
    {
        return Rt::compose(self::FORM_FORM_MESSAGE, [], (string) ($in['class'] ?? ''));
    }

    /** Fieldset class composition — the server-side asChild substitute. */
    public static function fieldsetClasses(array $in = []): string
    {
        return Rt::compose(self::CONTROLS_FIELDSET, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** Legend class composition — the server-side asChild substitute. */
    public static function legendClasses(array $in = []): string
    {
        return Rt::compose(self::CONTROLS_LEGEND, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** DataList class composition — the server-side asChild substitute. */
    public static function dataListClasses(array $in = []): string
    {
        return Rt::cn((string) ($in['class'] ?? ''));
    }

    /** Output class composition — the server-side asChild substitute. */
    public static function outputClasses(array $in = []): string
    {
        return Rt::cn((string) ($in['class'] ?? ''));
    }

    /** Meter class composition — the server-side asChild substitute. */
    public static function meterClasses(array $in = []): string
    {
        return Rt::compose(self::CONTROLS_METER, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** Progress class composition — the server-side asChild substitute. */
    public static function progressClasses(array $in = []): string
    {
        return Rt::compose(self::CONTROLS_PROGRESS, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** Grid class composition — the server-side asChild substitute. */
    public static function gridClasses(array $in = []): string
    {
        return Rt::compose(self::GRID_GRID, [], ((str_contains((string) ($in['class'] ?? ''), 'grid-cols-')) ? '' : \UI8Kit\Rt::mapOr(\UI8Kit\Rt::defaultIfEmpty((string) ($in['cols'] ?? ''), '1'), ['1' => 'grid-cols-1', '2' => 'grid-cols-2', '3' => 'grid-cols-3', '4' => 'grid-cols-4', '5' => 'grid-cols-5', '6' => 'grid-cols-6', '7' => 'grid-cols-7', '8' => 'grid-cols-8', '9' => 'grid-cols-9', '10' => 'grid-cols-10', '11' => 'grid-cols-11', '12' => 'grid-cols-12', '1-2' => 'grid-cols-1 md:grid-cols-2', '1-3' => 'grid-cols-1 md:grid-cols-2 xl:grid-cols-3', '1-4' => 'grid-cols-1 md:grid-cols-2 xl:grid-cols-4'], '')), (string) ($in['class'] ?? ''));
    }

    /** GridCol class composition — the server-side asChild substitute. */
    public static function gridColClasses(array $in = []): string
    {
        return Rt::cn(\UI8Kit\Rt::intMapOr((int) ($in['span'] ?? 0), [1 => 'col-span-1', 2 => 'col-span-2', 3 => 'col-span-3', 4 => 'col-span-4', 5 => 'col-span-5', 6 => 'col-span-6', 7 => 'col-span-7', 8 => 'col-span-8', 9 => 'col-span-9', 10 => 'col-span-10', 11 => 'col-span-11', 12 => 'col-span-12'], ''), \UI8Kit\Rt::intMapOr((int) ($in['start'] ?? 0), [1 => 'col-start-1', 2 => 'col-start-2', 3 => 'col-start-3', 4 => 'col-start-4', 5 => 'col-start-5', 6 => 'col-start-6', 7 => 'col-start-7', 8 => 'col-start-8', 9 => 'col-start-9', 10 => 'col-start-10', 11 => 'col-start-11', 12 => 'col-start-12'], ''), \UI8Kit\Rt::intMapOr((int) ($in['end'] ?? 0), [1 => 'col-end-1', 2 => 'col-end-2', 3 => 'col-end-3', 4 => 'col-end-4', 5 => 'col-end-5', 6 => 'col-end-6', 7 => 'col-end-7', 8 => 'col-end-8', 9 => 'col-end-9', 10 => 'col-end-10', 11 => 'col-end-11', 12 => 'col-end-12'], ''), \UI8Kit\Rt::intMapOr((int) ($in['order'] ?? 0), [1 => 'order-1', 2 => 'order-2', 3 => 'order-3', 4 => 'order-4', 5 => 'order-5', 6 => 'order-6', 7 => 'order-7', 8 => 'order-8', 9 => 'order-9', 10 => 'order-10', 11 => 'order-11', 12 => 'order-12'], ''), (string) ($in['class'] ?? ''));
    }

    /** Group class composition — the server-side asChild substitute. */
    public static function groupClasses(array $in = []): string
    {
        return Rt::compose(self::GROUP_GROUP, [], (string) ($in['class'] ?? ''));
    }

    /** Icon class composition — the server-side asChild substitute. */
    public static function iconClasses(array $in = []): string
    {
        return Rt::compose(self::ICON_ICON, ['type' => \UI8Kit\Rt::oneOfOr((string) ($in['type'] ?? ''), 'class', 'svg', 'text'), 'size' => (string) ($in['size'] ?? '')], (((\UI8Kit\Rt::oneOfOr((string) ($in['type'] ?? ''), 'class', 'svg', 'text') === 'class')) ? (string) ($in['baseClass'] ?? '') : ''), ((((\UI8Kit\Rt::oneOfOr((string) ($in['type'] ?? ''), 'class', 'svg', 'text') === 'class') && \UI8Kit\Rt::isSetStr((string) ($in['name'] ?? '')))) ? \UI8Kit\Rt::concatTrim((string) ($in['prefix'] ?? ''), (string) ($in['name'] ?? '')) : ''), (string) ($in['class'] ?? ''));
    }

    /** IconBadge class composition — the server-side asChild substitute. */
    public static function iconBadgeClasses(array $in = []): string
    {
        return Rt::compose(self::ICONBADGE_ICONBADGE, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** Image class composition — the server-side asChild substitute. */
    public static function imageClasses(array $in = []): string
    {
        return Rt::compose(self::IMAGE_IMAGE, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? ''), 'fit' => (string) ($in['fit'] ?? ''), 'position' => (string) ($in['position'] ?? ''), 'aspect' => (string) ($in['aspect'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** Picture class composition — the server-side asChild substitute. */
    public static function pictureClasses(array $in = []): string
    {
        return Rt::cn((string) ($in['class'] ?? ''));
    }

    /** Inline class composition — the server-side asChild substitute. */
    public static function inlineClasses(array $in = []): string
    {
        return Rt::compose(self::INLINE_INLINE, [], (string) ($in['class'] ?? ''));
    }

    /** Input class composition — the server-side asChild substitute. */
    public static function inputClasses(array $in = []): string
    {
        return Rt::compose(self::INPUT_INPUT, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** Label class composition — the server-side asChild substitute. */
    public static function labelClasses(array $in = []): string
    {
        return Rt::compose(self::LABEL_LABEL, [], (string) ($in['class'] ?? ''));
    }

    /** Break class composition — the server-side asChild substitute. */
    public static function breakClasses(array $in = []): string
    {
        return Rt::compose(self::LINEBREAK_LINEBREAK, [], (string) ($in['class'] ?? ''));
    }

    /** Link class composition — the server-side asChild substitute. */
    public static function linkClasses(array $in = []): string
    {
        return Rt::compose(self::LINK_LINK, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (((bool) ($in['disabled'] ?? false)) ? 'pointer-events-none opacity-50' : ''), (string) ($in['class'] ?? ''));
    }

    /** List class composition — the server-side asChild substitute. */
    public static function listClasses(array $in = []): string
    {
        return Rt::compose(self::LIST_LIST, [], (string) ($in['class'] ?? ''));
    }

    /** ListItem class composition — the server-side asChild substitute. */
    public static function listItemClasses(array $in = []): string
    {
        return Rt::compose(self::LIST_LIST, [], (string) ($in['class'] ?? ''));
    }

    /** Radio class composition — the server-side asChild substitute. */
    public static function radioClasses(array $in = []): string
    {
        return Rt::compose(self::RADIO_RADIO, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** Select class composition — the server-side asChild substitute. */
    public static function selectClasses(array $in = []): string
    {
        return Rt::compose(self::SELECT_SELECT, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** OptGroup class composition — the server-side asChild substitute. */
    public static function optGroupClasses(array $in = []): string
    {
        return Rt::cn((string) ($in['class'] ?? ''));
    }

    /** Separator class composition — the server-side asChild substitute. */
    public static function separatorClasses(array $in = []): string
    {
        return Rt::compose(self::SEPARATOR_SEPARATOR, ['variant' => (string) ($in['variant'] ?? ''), 'orientation' => (string) ($in['orientation'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** Stack class composition — the server-side asChild substitute. */
    public static function stackClasses(array $in = []): string
    {
        return Rt::compose(self::STACK_STACK, [], (string) ($in['class'] ?? ''));
    }

    /** Switch class composition — the server-side asChild substitute. */
    public static function switchClasses(array $in = []): string
    {
        return Rt::compose(self::SWITCH_SWITCH, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** Table class composition — the server-side asChild substitute. */
    public static function tableClasses(array $in = []): string
    {
        return Rt::compose(self::TABLE_TABLE, [], (string) ($in['class'] ?? ''));
    }

    /** TableCaption class composition — the server-side asChild substitute. */
    public static function tableCaptionClasses(array $in = []): string
    {
        return Rt::cn((string) ($in['class'] ?? ''));
    }

    /** TableHead class composition — the server-side asChild substitute. */
    public static function tableHeadClasses(array $in = []): string
    {
        return Rt::cn((string) ($in['class'] ?? ''));
    }

    /** TableBody class composition — the server-side asChild substitute. */
    public static function tableBodyClasses(array $in = []): string
    {
        return Rt::cn((string) ($in['class'] ?? ''));
    }

    /** TableFoot class composition — the server-side asChild substitute. */
    public static function tableFootClasses(array $in = []): string
    {
        return Rt::cn((string) ($in['class'] ?? ''));
    }

    /** TableRow class composition — the server-side asChild substitute. */
    public static function tableRowClasses(array $in = []): string
    {
        return Rt::cn((string) ($in['class'] ?? ''));
    }

    /** TableHeadCell class composition — the server-side asChild substitute. */
    public static function tableHeadCellClasses(array $in = []): string
    {
        return Rt::cn((string) ($in['class'] ?? ''));
    }

    /** TableCell class composition — the server-side asChild substitute. */
    public static function tableCellClasses(array $in = []): string
    {
        return Rt::cn((string) ($in['class'] ?? ''));
    }

    /** TableColGroup class composition — the server-side asChild substitute. */
    public static function tableColGroupClasses(array $in = []): string
    {
        return Rt::cn((string) ($in['class'] ?? ''));
    }

    /** TableCol class composition — the server-side asChild substitute. */
    public static function tableColClasses(array $in = []): string
    {
        return Rt::cn((string) ($in['class'] ?? ''));
    }

    /** Text class composition — the server-side asChild substitute. */
    public static function textClasses(array $in = []): string
    {
        return Rt::compose(self::TEXT_TEXT, [], (string) ($in['class'] ?? ''));
    }

    /** Textarea class composition — the server-side asChild substitute. */
    public static function textareaClasses(array $in = []): string
    {
        return Rt::compose(self::TEXTAREA_TEXTAREA, ['variant' => (string) ($in['variant'] ?? ''), 'size' => (string) ($in['size'] ?? '')], (string) ($in['class'] ?? ''));
    }

    /** Title class composition — the server-side asChild substitute. */
    public static function titleClasses(array $in = []): string
    {
        return Rt::compose(self::TITLE_TITLE, [], (string) ($in['class'] ?? ''));
    }
}
