// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Badge shows a short status label.
Badge uses variant and size presets.
*/

package badge

import (
	uiutils "github.com/ui8kit/ui/utils"
)

// BadgeProps configures Badge.
type BadgeProps struct {
	// Variant — color preset: default, secondary, destructive, outline
	Variant string
	// Size — density preset: default, sm, lg
	Size string
	// Class — adds Tailwind utilities
	Class string
	Attrs templ.Attributes
}

// BadgeClasses composes Badge classes without rendering —
// the server-composition escape hatch for custom root elements.
func BadgeClasses(p BadgeProps) string {
	return uiutils.Compose(BadgeVariants, map[string]string{
		"variant": p.Variant,
		"size": p.Size,
	}, p.Class)
}

// Badge renders the Badge contract.
templ Badge(p BadgeProps) {
	<div
		class={ BadgeClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</div>
}
