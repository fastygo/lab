// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Stack arranges children vertically in a flex column.
Default layout primitive for spacing sections and form fields.
*/

package stack

import (
	uiutils "github.com/ui8kit/ui/utils"
)

// StackProps configures Stack.
type StackProps struct {
	// Class — adds Tailwind utilities
	Class string
	// Tag — div, ul, or ol. Use Block for landmarks (aside, section, header, nav).
	Tag string
	Attrs templ.Attributes
}

// StackClasses composes Stack classes without rendering —
// the server-composition escape hatch for custom root elements.
func StackClasses(p StackProps) string {
	return uiutils.Compose(StackVariants, map[string]string{}, p.Class)
}

// Stack renders the Stack contract.
templ Stack(p StackProps) {
	switch uiutils.ResolveTag(p.Tag, "div", uiutils.TagGroupStack) {
		case "ul":
			<ul
				class={ StackClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</ul>
		case "ol":
			<ol
				class={ StackClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</ol>
		default:
			<div
				class={ StackClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</div>
	}
}
