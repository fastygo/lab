// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
List renders semantic ul, ol, dl, or menu lists with ListItem children.
*/

package list

import (
	"strconv"

	uiutils "github.com/ui8kit/ui/utils"
)

// ListProps configures List.
type ListProps struct {
	// Class — adds Tailwind utilities
	Class string
	// Tag — ul, ol, dl, or menu
	Tag string
	Attrs templ.Attributes
}

// ListClasses composes List classes without rendering —
// the server-composition escape hatch for custom root elements.
func ListClasses(p ListProps) string {
	return uiutils.Compose(ListVariants, map[string]string{}, p.Class)
}

// List renders the List contract.
templ List(p ListProps) {
	switch uiutils.ResolveTag(p.Tag, "ul", uiutils.TagGroupList) {
		case "ol":
			<ol
				class={ ListClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</ol>
		case "dl":
			<dl
				class={ ListClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</dl>
		case "menu":
			<menu
				class={ ListClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</menu>
		default:
			<ul
				class={ ListClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</ul>
	}
}

// ListItemProps configures ListItem.
type ListItemProps struct {
	// Class — adds Tailwind utilities
	Class string
	// Tag — li, dt, or dd; defaults to li
	Tag string
	// Value — optional li value for ordered contexts
	Value int
	Attrs templ.Attributes
}

// ListItemClasses composes ListItem classes without rendering —
// the server-composition escape hatch for custom root elements.
func ListItemClasses(p ListItemProps) string {
	return uiutils.Compose(ListVariants, map[string]string{}, p.Class)
}

// ListItem renders the ListItem contract.
templ ListItem(p ListItemProps) {
	switch uiutils.ResolveTag(p.Tag, "li", uiutils.TagGroupListItem) {
		case "dt":
			<dt
				class={ ListItemClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</dt>
		case "dd":
			<dd
				class={ ListItemClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</dd>
		default:
			<li
				class={ ListItemClasses(p) }
				if (int(p.Value) > 0) {
					value={ strconv.Itoa(int(p.Value)) }
				}
				{ p.Attrs... }
			>
				{ children... }
			</li>
	}
}
