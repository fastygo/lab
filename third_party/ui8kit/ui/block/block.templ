// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Block is a top-level page landmark (main, header, section, nav, …).
Use for document structure; do not nest Block inside Block.
*/

package block

import (
	uiutils "github.com/ui8kit/ui/utils"
)

// BlockProps configures Block.
type BlockProps struct {
	ID string
	// Class — adds Tailwind utilities
	Class string
	// Tag — semantic landmark: main, header, section, footer, nav, article, aside, figure
	Tag string
	Attrs templ.Attributes
}

// BlockClasses composes Block classes without rendering —
// the server-composition escape hatch for custom root elements.
func BlockClasses(p BlockProps) string {
	return uiutils.Compose(BlockVariants, map[string]string{}, p.Class)
}

// Block renders the Block contract.
templ Block(p BlockProps) {
	switch uiutils.ResolveTag(p.Tag, "div", uiutils.TagGroupLayout) {
		case "section":
			<section
				class={ BlockClasses(p) }
				if uiutils.IsSetStr(p.ID) {
					id={ p.ID }
				}
				{ p.Attrs... }
			>
				{ children... }
			</section>
		case "article":
			<article
				class={ BlockClasses(p) }
				if uiutils.IsSetStr(p.ID) {
					id={ p.ID }
				}
				{ p.Attrs... }
			>
				{ children... }
			</article>
		case "aside":
			<aside
				class={ BlockClasses(p) }
				if uiutils.IsSetStr(p.ID) {
					id={ p.ID }
				}
				{ p.Attrs... }
			>
				{ children... }
			</aside>
		case "header":
			<header
				class={ BlockClasses(p) }
				if uiutils.IsSetStr(p.ID) {
					id={ p.ID }
				}
				{ p.Attrs... }
			>
				{ children... }
			</header>
		case "footer":
			<footer
				class={ BlockClasses(p) }
				if uiutils.IsSetStr(p.ID) {
					id={ p.ID }
				}
				{ p.Attrs... }
			>
				{ children... }
			</footer>
		case "main":
			<main
				class={ BlockClasses(p) }
				if uiutils.IsSetStr(p.ID) {
					id={ p.ID }
				}
				{ p.Attrs... }
			>
				{ children... }
			</main>
		case "nav":
			<nav
				class={ BlockClasses(p) }
				if uiutils.IsSetStr(p.ID) {
					id={ p.ID }
				}
				{ p.Attrs... }
			>
				{ children... }
			</nav>
		case "figure":
			<figure
				class={ BlockClasses(p) }
				if uiutils.IsSetStr(p.ID) {
					id={ p.ID }
				}
				{ p.Attrs... }
			>
				{ children... }
			</figure>
		case "search":
			<search
				class={ BlockClasses(p) }
				if uiutils.IsSetStr(p.ID) {
					id={ p.ID }
				}
				{ p.Attrs... }
			>
				{ children... }
			</search>
		case "hgroup":
			<hgroup
				class={ BlockClasses(p) }
				if uiutils.IsSetStr(p.ID) {
					id={ p.ID }
				}
				{ p.Attrs... }
			>
				{ children... }
			</hgroup>
		default:
			<div
				class={ BlockClasses(p) }
				if uiutils.IsSetStr(p.ID) {
					id={ p.ID }
				}
				{ p.Attrs... }
			>
				{ children... }
			</div>
	}
}
