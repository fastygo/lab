// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Grid lays out children in a CSS grid with optional column wrappers.
*/

package grid

import (
	"strings"

	uiutils "github.com/ui8kit/ui/utils"
)

// GridProps configures Grid.
type GridProps struct {
	// Class — adds Tailwind utilities
	Class string
	// Cols — column preset: 1–12 or responsive 1-2, 1-3, 1-4
	Cols string
	Attrs templ.Attributes
}

// GridClasses composes Grid classes without rendering —
// the server-composition escape hatch for custom root elements.
func GridClasses(p GridProps) string {
	return uiutils.Compose(GridVariants, map[string]string{}, uiutils.If(strings.Contains(p.Class, "grid-cols-"), "", uiutils.MapOr(uiutils.DefaultIfEmpty(p.Cols, "1"), map[string]string{"1": "grid-cols-1", "2": "grid-cols-2", "3": "grid-cols-3", "4": "grid-cols-4", "5": "grid-cols-5", "6": "grid-cols-6", "7": "grid-cols-7", "8": "grid-cols-8", "9": "grid-cols-9", "10": "grid-cols-10", "11": "grid-cols-11", "12": "grid-cols-12", "1-2": "grid-cols-1 md:grid-cols-2", "1-3": "grid-cols-1 md:grid-cols-2 xl:grid-cols-3", "1-4": "grid-cols-1 md:grid-cols-2 xl:grid-cols-4"}, "")), p.Class)
}

// Grid renders the Grid contract.
templ Grid(p GridProps) {
	<div
		class={ GridClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</div>
}

// GridColProps configures GridCol.
type GridColProps struct {
	// Class — adds Tailwind utilities
	Class string
	// Span — col-span-1..12
	Span int
	// Start — col-start-1..12
	Start int
	// End — col-end-1..12
	End int
	// Order — order-1..12
	Order int
	Attrs templ.Attributes
}

// GridColClasses composes GridCol classes without rendering —
// the server-composition escape hatch for custom root elements.
func GridColClasses(p GridColProps) string {
	return uiutils.Cn(uiutils.IntMapOr(int(p.Span), map[int]string{1: "col-span-1", 2: "col-span-2", 3: "col-span-3", 4: "col-span-4", 5: "col-span-5", 6: "col-span-6", 7: "col-span-7", 8: "col-span-8", 9: "col-span-9", 10: "col-span-10", 11: "col-span-11", 12: "col-span-12"}, ""), uiutils.IntMapOr(int(p.Start), map[int]string{1: "col-start-1", 2: "col-start-2", 3: "col-start-3", 4: "col-start-4", 5: "col-start-5", 6: "col-start-6", 7: "col-start-7", 8: "col-start-8", 9: "col-start-9", 10: "col-start-10", 11: "col-start-11", 12: "col-start-12"}, ""), uiutils.IntMapOr(int(p.End), map[int]string{1: "col-end-1", 2: "col-end-2", 3: "col-end-3", 4: "col-end-4", 5: "col-end-5", 6: "col-end-6", 7: "col-end-7", 8: "col-end-8", 9: "col-end-9", 10: "col-end-10", 11: "col-end-11", 12: "col-end-12"}, ""), uiutils.IntMapOr(int(p.Order), map[int]string{1: "order-1", 2: "order-2", 3: "order-3", 4: "order-4", 5: "order-5", 6: "order-6", 7: "order-7", 8: "order-8", 9: "order-9", 10: "order-10", 11: "order-11", 12: "order-12"}, ""), p.Class)
}

// GridCol renders the GridCol contract.
templ GridCol(p GridColProps) {
	<div
		class={ GridColClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</div>
}
