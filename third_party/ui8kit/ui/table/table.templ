// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Table composes semantic table, caption, sections, rows, and cells.
Supports scope, colspan, and headers attrs on head cells.
*/

package table

import (
	"strconv"

	uiutils "github.com/ui8kit/ui/utils"
)

// TableProps configures Table.
type TableProps struct {
	// Class — adds Tailwind utilities
	Class string
	Attrs templ.Attributes
}

// TableClasses composes Table classes without rendering —
// the server-composition escape hatch for custom root elements.
func TableClasses(p TableProps) string {
	return uiutils.Compose(TableVariants, map[string]string{}, p.Class)
}

// Table renders the Table contract.
templ Table(p TableProps) {
	<table
		class={ TableClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</table>
}

// TableCaptionProps configures TableCaption.
type TableCaptionProps struct {
	// Class — adds Tailwind utilities
	Class string
	Attrs templ.Attributes
}

// TableCaptionClasses composes TableCaption classes without rendering —
// the server-composition escape hatch for custom root elements.
func TableCaptionClasses(p TableCaptionProps) string {
	return uiutils.Cn(p.Class)
}

// TableCaption renders the TableCaption contract.
templ TableCaption(p TableCaptionProps) {
	<caption
		class={ TableCaptionClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</caption>
}

// TableHeadProps configures TableHead.
type TableHeadProps struct {
	// Class — adds Tailwind utilities
	Class string
	Attrs templ.Attributes
}

// TableHeadClasses composes TableHead classes without rendering —
// the server-composition escape hatch for custom root elements.
func TableHeadClasses(p TableHeadProps) string {
	return uiutils.Cn(p.Class)
}

// TableHead renders the TableHead contract.
templ TableHead(p TableHeadProps) {
	<thead
		class={ TableHeadClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</thead>
}

// TableBodyProps configures TableBody.
type TableBodyProps struct {
	// Class — adds Tailwind utilities
	Class string
	Attrs templ.Attributes
}

// TableBodyClasses composes TableBody classes without rendering —
// the server-composition escape hatch for custom root elements.
func TableBodyClasses(p TableBodyProps) string {
	return uiutils.Cn(p.Class)
}

// TableBody renders the TableBody contract.
templ TableBody(p TableBodyProps) {
	<tbody
		class={ TableBodyClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</tbody>
}

// TableFootProps configures TableFoot.
type TableFootProps struct {
	// Class — adds Tailwind utilities
	Class string
	Attrs templ.Attributes
}

// TableFootClasses composes TableFoot classes without rendering —
// the server-composition escape hatch for custom root elements.
func TableFootClasses(p TableFootProps) string {
	return uiutils.Cn(p.Class)
}

// TableFoot renders the TableFoot contract.
templ TableFoot(p TableFootProps) {
	<tfoot
		class={ TableFootClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</tfoot>
}

// TableRowProps configures TableRow.
type TableRowProps struct {
	// Class — adds Tailwind utilities
	Class string
	Attrs templ.Attributes
}

// TableRowClasses composes TableRow classes without rendering —
// the server-composition escape hatch for custom root elements.
func TableRowClasses(p TableRowProps) string {
	return uiutils.Cn(p.Class)
}

// TableRow renders the TableRow contract.
templ TableRow(p TableRowProps) {
	<tr
		class={ TableRowClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</tr>
}

// TableHeadCellProps configures TableHeadCell.
type TableHeadCellProps struct {
	// Class — adds Tailwind utilities
	Class string
	// Scope — th scope: col, row, colgroup, rowgroup
	Scope string
	ColSpan int
	RowSpan int
	// Headers — ids of related header cells
	Headers string
	// Abbr — abbreviated header text for th
	Abbr string
	Attrs templ.Attributes
}

// TableHeadCellClasses composes TableHeadCell classes without rendering —
// the server-composition escape hatch for custom root elements.
func TableHeadCellClasses(p TableHeadCellProps) string {
	return uiutils.Cn(p.Class)
}

// TableHeadCell renders the TableHeadCell contract.
templ TableHeadCell(p TableHeadCellProps) {
	<th
		class={ TableHeadCellClasses(p) }
		if uiutils.IsSetStr(uiutils.OneOfOrLower(p.Scope, "", "col", "colgroup", "row", "rowgroup")) {
			scope={ uiutils.OneOfOrLower(p.Scope, "", "col", "colgroup", "row", "rowgroup") }
		}
		if uiutils.IsSetStr(p.Abbr) {
			abbr={ p.Abbr }
		}
		if (int(p.ColSpan) > 0) {
			colspan={ strconv.Itoa(int(p.ColSpan)) }
		}
		if (int(p.RowSpan) > 0) {
			rowspan={ strconv.Itoa(int(p.RowSpan)) }
		}
		if uiutils.IsSetStr(p.Headers) {
			headers={ p.Headers }
		}
		{ p.Attrs... }
	>
		{ children... }
	</th>
}

// TableCellProps configures TableCell.
type TableCellProps struct {
	// Class — adds Tailwind utilities
	Class string
	// Scope — th scope: col, row, colgroup, rowgroup
	Scope string
	ColSpan int
	RowSpan int
	// Headers — ids of related header cells
	Headers string
	// Abbr — abbreviated header text for th
	Abbr string
	Attrs templ.Attributes
}

// TableCellClasses composes TableCell classes without rendering —
// the server-composition escape hatch for custom root elements.
func TableCellClasses(p TableCellProps) string {
	return uiutils.Cn(p.Class)
}

// TableCell renders the TableCell contract.
templ TableCell(p TableCellProps) {
	<td
		class={ TableCellClasses(p) }
		if (int(p.ColSpan) > 0) {
			colspan={ strconv.Itoa(int(p.ColSpan)) }
		}
		if (int(p.RowSpan) > 0) {
			rowspan={ strconv.Itoa(int(p.RowSpan)) }
		}
		if uiutils.IsSetStr(p.Headers) {
			headers={ p.Headers }
		}
		{ p.Attrs... }
	>
		{ children... }
	</td>
}

// TableColGroupProps configures TableColGroup.
type TableColGroupProps struct {
	// Class — adds Tailwind utilities
	Class string
	Span int
	Attrs templ.Attributes
}

// TableColGroupClasses composes TableColGroup classes without rendering —
// the server-composition escape hatch for custom root elements.
func TableColGroupClasses(p TableColGroupProps) string {
	return uiutils.Cn(p.Class)
}

// TableColGroup renders the TableColGroup contract.
templ TableColGroup(p TableColGroupProps) {
	<colgroup
		class={ TableColGroupClasses(p) }
		if (int(p.Span) > 0) {
			span={ strconv.Itoa(int(p.Span)) }
		}
		{ p.Attrs... }
	>
		{ children... }
	</colgroup>
}

// TableColProps configures TableCol.
type TableColProps struct {
	// Class — adds Tailwind utilities
	Class string
	Span int
	Attrs templ.Attributes
}

// TableColClasses composes TableCol classes without rendering —
// the server-composition escape hatch for custom root elements.
func TableColClasses(p TableColProps) string {
	return uiutils.Cn(p.Class)
}

// TableCol renders the TableCol contract.
templ TableCol(p TableColProps) {
	<col
		class={ TableColClasses(p) }
		if (int(p.Span) > 0) {
			span={ strconv.Itoa(int(p.Span)) }
		}
		{ p.Attrs... }
	/>
}
