// Code generated by @ui8kit/codegen. DO NOT EDIT.

/*
Card groups related content in one bordered surface.
Card uses header, content, and footer slots.
For semantic root elements (section, article), use CardClasses on a manual wrapper.
*/

package card

import (
	uiutils "github.com/ui8kit/ui/utils"
)

// CardProps configures Card.
type CardProps struct {
	// Class — adds Tailwind utilities
	Class string
	// Variant — surface preset: default, raised, kpi, muted, ghost, compact, flat, accent
	Variant string
	Attrs templ.Attributes
}

// CardClasses composes Card classes without rendering —
// the server-composition escape hatch for custom root elements.
func CardClasses(p CardProps) string {
	return uiutils.Compose(CardVariants, map[string]string{
		"variant": p.Variant,
	}, p.Class)
}

// Card renders the Card contract.
templ Card(p CardProps) {
	<div
		class={ CardClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</div>
}

// CardHeaderProps configures CardHeader.
type CardHeaderProps struct {
	// Class — adds Tailwind utilities
	Class string
	Attrs templ.Attributes
}

// CardHeaderClasses composes CardHeader classes without rendering —
// the server-composition escape hatch for custom root elements.
func CardHeaderClasses(p CardHeaderProps) string {
	return uiutils.Cn("border-b border-border px-4 py-2", p.Class)
}

// CardHeader renders the CardHeader contract.
templ CardHeader(p CardHeaderProps) {
	<div
		class={ CardHeaderClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</div>
}

// CardTitleProps configures CardTitle.
type CardTitleProps struct {
	// Class — adds Tailwind utilities
	Class string
	// As — maps to h1-h6 (default h2)
	As uiutils.HeadingLevel
	Attrs templ.Attributes
}

// CardTitleClasses composes CardTitle classes without rendering —
// the server-composition escape hatch for custom root elements.
func CardTitleClasses(p CardTitleProps) string {
	return uiutils.Cn("text-sm font-semibold", p.Class)
}

// CardTitle renders the CardTitle contract.
templ CardTitle(p CardTitleProps) {
	switch uiutils.TitleTag(p.As) {
		case "h1":
			<h1
				class={ CardTitleClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</h1>
		case "h3":
			<h3
				class={ CardTitleClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</h3>
		case "h4":
			<h4
				class={ CardTitleClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</h4>
		case "h5":
			<h5
				class={ CardTitleClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</h5>
		case "h6":
			<h6
				class={ CardTitleClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</h6>
		default:
			<h2
				class={ CardTitleClasses(p) }
				{ p.Attrs... }
			>
				{ children... }
			</h2>
	}
}

// CardDescriptionProps configures CardDescription.
type CardDescriptionProps struct {
	// Class — adds Tailwind utilities
	Class string
	Attrs templ.Attributes
}

// CardDescriptionClasses composes CardDescription classes without rendering —
// the server-composition escape hatch for custom root elements.
func CardDescriptionClasses(p CardDescriptionProps) string {
	return uiutils.Cn("text-sm text-muted-foreground", p.Class)
}

// CardDescription renders the CardDescription contract.
templ CardDescription(p CardDescriptionProps) {
	<p
		class={ CardDescriptionClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</p>
}

// CardContentProps configures CardContent.
type CardContentProps struct {
	// Class — adds Tailwind utilities
	Class string
	Attrs templ.Attributes
}

// CardContentClasses composes CardContent classes without rendering —
// the server-composition escape hatch for custom root elements.
func CardContentClasses(p CardContentProps) string {
	return uiutils.Cn("p-4", p.Class)
}

// CardContent renders the CardContent contract.
templ CardContent(p CardContentProps) {
	<div
		class={ CardContentClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</div>
}

// CardFooterProps configures CardFooter.
type CardFooterProps struct {
	// Class — adds Tailwind utilities
	Class string
	Attrs templ.Attributes
}

// CardFooterClasses composes CardFooter classes without rendering —
// the server-composition escape hatch for custom root elements.
func CardFooterClasses(p CardFooterProps) string {
	return uiutils.Cn("border-t border-border px-4 py-3", p.Class)
}

// CardFooter renders the CardFooter contract.
templ CardFooter(p CardFooterProps) {
	<div
		class={ CardFooterClasses(p) }
		{ p.Attrs... }
	>
		{ children... }
	</div>
}
